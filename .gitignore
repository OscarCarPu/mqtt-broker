mosquitto/data/*
mosquitto/log/*

# Local credentials
.env

# Runtime state owned by the services, not by git.
# secrets/passwd holds the password hashes (see make create-password); the web UI
# rewrites settings.json on every change and it would otherwise carry the broker
# password in plain text.
mosquitto/secrets/
mqtt-explorer/config/settings.json
