mosquitto/data/*
mosquitto/log/*

# Local credentials
.env

# Runtime state owned by the services, not by git.
# passwd holds password hashes (see make create-password); the web UI rewrites
# settings.json on every change and it would otherwise carry the broker
# password in plain text.
mosquitto/config/passwd
mqtt-explorer/config/settings.json
